import { createTheme } from "@mui/material"

export const RMBTheme = createTheme({
    palette : {
        primary : {
            main: '#005A99',
            contrastText: '#FFFFFF'
        }
    }
})
